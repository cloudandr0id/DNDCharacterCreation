package database_classes;

import static org.junit.Assert.*;

import org.junit.Test;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;

public class CharacterUnitTests
{
    Character character = new Character(None, None, None, None, None, None,
    None, None);

    @Test
    public void evaluateName()
    {
        character.setName("Billy Bob");
        assertEquals(account.getName(), "Billy Bob");
    }

    @Test
    public void evaluateRace()
    {
        [insert mock class]
    }
}
